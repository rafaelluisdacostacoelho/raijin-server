# Override ExternalSecrets para ambiente TST — apontar para paths do Vault de staging
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: database-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: database-credentials
    creationPolicy: Owner
  data:
    - secretKey: POSTGRES_USER
      remoteRef:
        key: secret/app/tst/database
        property: username
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: secret/app/tst/database
        property: password
    - secretKey: POSTGRES_DB
      remoteRef:
        key: secret/app/tst/database
        property: database
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: auth-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: auth-secrets
    creationPolicy: Owner
  data:
    - secretKey: JWT_SECRET
      remoteRef:
        key: secret/app/tst/auth
        property: jwt_secret
